const toggleButtonInfo = document.getElementById('toggleButtonInfo');

        toggleButtonInfo.addEventListener('click', function() {
            toggleButtonInfo.classList.toggle('active-info');
        });